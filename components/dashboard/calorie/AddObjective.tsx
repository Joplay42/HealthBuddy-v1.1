/**
 * This component is the add objective content to change the user objective, it is
 * used with a modal pop-up using the Modal component.
 *
 * @returns
 */
const AddObjective = () => {
  return <div>AddObjective</div>;
};

export default AddObjective;
